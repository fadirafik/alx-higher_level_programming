#!/usr/bin/python3
"""
is same class module
"""


def is_same_class(obj, a_class):
    """checks if objecr is an instance of a class"""\

    if type(obj) is a_class:
        return True
    return False
